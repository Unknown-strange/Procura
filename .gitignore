# OS / editor
.DS_Store
Thumbs.db
desktop.ini
*.log
.idea/
.vscode/
.cursor/

# Env secrets (never commit)
.env
.env.*
!.env.example
!.env.local.example
!.env*.example

# Nested app build artifacts
procura/node_modules/
procura/.next/
procura/out/
procura/.vercel/
procura/supabase/.temp/

# Large reference data (do not push to GitHub)
# Do not use a bare `data/` rule — it also ignores procura/lib/data/
*.xlsx
*.xls
UNGM_UNSPSC*
/data
/data/raw/

# Internal planning / design references (not for public repo)
BUILD_PLAN.md
BUILD_CHECKLIST.md
DESIGN_SYSTEM.md
ui_procura/
